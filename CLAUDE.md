# CLAUDE.md — photolibre

## プロジェクト概要

Apple Photos/iPhotoの独自仕様から写真を解放し、オープンな形式（SQLite + XMPサイドカー）で管理する、クロスプラットフォーム対応の写真アーカイブ・閲覧ツール。「Apple Photos脱出プロジェクト」（スクール卒業制作）の成果物。

---

## 技術スタック

| 項目 | 内容 |
|---|---|
| インポーター（`importer/`） | Python 3.11+ / sqlite3 / Pillow / python-xmp-toolkit / tqdm |
| 閲覧アプリ（`viewer/`） | Tauri 2.x (Rust) + React 18 + TypeScript |
| バージョン管理 | GitHub |

モノレポ構成。`archive.db`のスキーマをインポーターと閲覧アプリで常に一致させるため、1リポジトリで管理する。

---

## データの扱い（重要）

- 実際の写真ファイル・`archive.db`は、このリポジトリの**外側**（ユーザーが指定する`archive/`フォルダ）に置く。個人の実写真をリポジトリに含めることは絶対にしない
- `archive/`フォルダは`archive_config.json`を含めて自己完結させ、インポーターと閲覧アプリの両方がこのフォルダを参照する
- テスト用のフィクスチャデータも、実在する個人写真ではなくダミーデータのみ使用する

---

## Git 運用ルール

### 基本方針
- **コードを変更するたびに GitHub へプッシュすること。**
- コミットとプッシュはセットで行う。変更をローカルに溜めない。

### コミット手順
1. 変更ファイルを確認: `git status`
2. 関連ファイルを個別にステージ: `git add <file>`（`git add .` は機密ファイル混入防止のため避ける）
3. コミット: `git commit -m "簡潔なメッセージ"`
4. プッシュ: `git push origin main`

### コミットメッセージ規則
- 日本語・英語どちらでもよい
- 内容を端的に表す1行メッセージ

### ブランチ
- 通常作業は `main` ブランチで行う

### 禁止事項
- `--force` プッシュは原則禁止（ユーザーが明示的に指示した場合のみ）
- 個人の実写真データ・`archive/`フォルダの中身をコミットしない
- `.env` をコミットしない

---

## リモートリポジトリ

- **GitHub**: https://github.com/ergtronix/photolibre
- デフォルトブランチ: `main`
- **現在は private。TASK-060（GitHub公開）のタイミングでpublicに切り替える予定。**
